<h1>All Pages</h1>

<table>
	<tr>
		<th>Page Name</th>
		<th></th>
		<th></th>
		<th></th>
	</tr>

	<% @pages.each do |page| %>
	  <tr>
	  	<td><%= page.title %></td>
	  	<td><%= link_to 'Show', page %></td>
	  	<td><%= link_to 'Edit', edit_admin_page_path(page) %></td>
	  	<td><%= link_to 'Destroy', page, confirm: 'Are you sure you want to delete this page?', method: :delete %></td>
	  </tr>
	  <% end %>
	</table>
	<%= link_to 'New Page', new_admin_page_path %>